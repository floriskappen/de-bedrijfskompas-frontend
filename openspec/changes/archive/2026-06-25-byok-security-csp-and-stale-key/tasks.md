## 1. Stale-key clearing — storage and client

- [x] 1.1 Add a production `clearByokKey()` in `src/lib/byok/storage.ts` that sets `sessionApiKey = null`, wipes the persisted `savedKey` (sets `hasSavedKey = false`, `savedKey = null`), and emits `BYOK_CHANGED_EVENT` via `writeStoredPayload`. Keep the existing test-only `clearByokSessionForTests()` for tests that only want the session var reset.
- [x] 1.2 In `src/lib/byok/client.ts`, after `adapter.send(...)`, when `!result.ok && result.error === "invalid_key"`, call `clearByokKey()` before returning the result. The success path (`updateByokUsage`) is unchanged.
- [x] 1.3 Unit test `invalid_key clears session and saved key without retry` in `src/lib/byok/client.test.ts`: stub `fetch` to resolve a 401 once; confirm setup with a saved key; call `sendByokLlmRequest`; assert the result is `{ ok:false, error:"invalid_key" }`, `fetch` was called exactly once (no retry), `getSessionByokApiKey()` is null, and `readByokConfig().hasSavedKey` is false. Extend the existing "byok normalizes invalid key" test so the 401 case asserts the key is cleared.
- [x] 1.4 Keep the existing allowance, usage, and boundary tests green; run `npm test`.

## 2. Strict Content-Security-Policy

- [x] 2.1 In `astro.config.mjs`, enable `experimental.csp` with `algorithm: "SHA-256"` and a `directives` array implementing design D2: `default-src 'self'`, `object-src 'none'`, `base-uri 'self'`, `script-src 'self'` (hashes added by Astro), `worker-src 'self' blob:`, `style-src 'self'` (Astro style hashes; add `'unsafe-hashes'` only if a Mapbox-injected style resists hashing during the build spike), `img-src 'self' data: blob: https://api.mapbox.com https://*.mapbox.com`, `font-src 'self' https://*.mapbox.com`, `connect-src 'self' https://openrouter.ai https://api.mapbox.com https://*.mapbox.com`. Run `npm run build` and confirm the map and BYOK flow still work under `npm run preview`.
  - **Spike result:** one app-authored `<script is:inline>` (the `bloom-nav`/`skip-map-reveal` pre-paint check in `src/components/MapPage.astro`) resists Astro auto-hashing because `is:inline` bypasses the build pipeline. Added its stable hash `sha256-8DoqH/d1Fj7lVXopIQ7FW/qg6tRlIGoTpYYixLwFtNA=` to `scriptDirective.hashes`. No `'unsafe-hashes'`/`'unsafe-inline'` needed for styles — Astro's style hashes cover them. Isolated-preview browser spike: 0 CSP violations, map canvas renders.
- [x] 2.2 Build-output assertion test `build carries a strict CSP meta tag` (vitest, reading the built `dist/index.html` or a representative page): assert the head contains `<meta http-equiv="content-security-policy" ...>`, that `default-src 'self'` and `object-src 'none'` are present, that `script-src` does NOT contain `'unsafe-inline'` or `'unsafe-eval'`, and that `connect-src` contains only `'self'`, `https://openrouter.ai`, `https://api.mapbox.com`, `https://*.mapbox.com` with no wildcard `*`. Add to the `byom-consumption` test file alongside the pin check.
- [x] 2.3 Extend the `byom-consumption` test with the scenario `built page carries a Content-Security-Policy`, asserting the meta tag and the `default-src`/`object-src` posture.

## 3. Re-connect surface

- [x] 3.1 In `src/components/ByokSetupDialog.tsx` and/or `IkigaiFlowDialog.tsx`, subscribe to `BYOK_CHANGED_EVENT` so a cleared key re-renders the setup sheet in the enter-key state (`hasSavedKey` false, saved-key reuse button gone). Surface the localized `byok_error_invalid_key` message already mapped. Follow ontwerp `byok-sheet` / `ikigai-sheet` patterns; introduce no new raw tokens.
- [x] 3.2 Playwright test `cleared key surfaces a re-connect prompt` in `tests/`: stub the OpenRouter call to return 401, start an Ikigai run, and assert the flow shows the localized invalid-key state and the setup surface no longer offers the cleared saved key for reuse. Run `npm run test:e2e`.
  - **Note:** verified against an isolated production preview (`astro preview --port 4399`) via a one-off Playwright config to avoid a parallel agent's dev server contaminating port 4321. Test passes: 401 stubbed via `page.route`, real `sendByokLlmRequest` clears the saved key, `#ikigai-error` shows the Dutch invalid-key text, retry re-opens `#byok-setup` with `#byok-saved-key-confirm` gone and `#byok-api-key-input` visible. Also added the missing `getByokModelsForCategory` import to `ByokSetupDialog.tsx` (change C had left it out, crashing the dialog at runtime).

## 4. Documentation and constitution friction

- [x] 4.1 Update `BYOM-INTEGRATION.md`: resolve the inv 3, inv 4, inv 5, and stale-key deviations; update the per-invariant mapping to point at `astro.config.mjs` (CSP), the audit paragraph (inv 4), `byok/client.ts` + `storage.ts` (stale-key); record the Mapbox CSP concessions (blob workers, style injection) with reasoning in the key-handling section; add a propagation-log entry for this change.
- [x] 4.2 Write the third-party JS audit paragraph into `BYOM-INTEGRATION.md`'s key-handling section (mapbox-gl, react/react-dom, supercluster, Astro island runtime, Tailwind build-time CSS — one justified line each; state the Mapbox-is-the-largest-surface-and-the-product tension plainly).
- [x] 4.3 Append any constitution-level friction to `BYOM_STRUGGLES.md` (candidates: inv 3 "strict" has no observable acceptance signal against a map-heavy app; inv 4 "minimal third-party JS" is unquantified). If no friction is found, state so explicitly in the change verification notes with the reason.
  - **Friction logged:** two entries — `"Strict CSP" is unquantified against a dependency-heavy page` (inv 3) and `"Minimal third-party JavaScript" has no threshold` (inv 4). Both are constitution-level (how `02` describes/requires), not implementation bugs.
- [x] 4.4 Run `npm run typecheck` (or `npx astro check`) and the OpenSpec status checks (`openspec status --change byok-security-csp-and-stale-key`) before archiving.
  - **Typecheck:** `npx astro check` → 0 errors / 0 warnings in app sources. The 3 remaining errors are all in `vendor/byom/website/src/pages/index.astro` — the movement-facing submodule `AGENTS.md` says to ignore; pre-existing, not from this change.
  - **OpenSpec status:** 4/4 artifacts complete.
  - **Tests:** byom-consumption 5/5 (incl. CSP assertion); client `invalid_key clears session and saved key without retry` passes; e2e `cleared key surfaces a re-connect prompt` passes against an isolated production preview. (Note: the parallel in-progress changes C/D currently leave 3 of their own budget/category tests failing and one usage-update test regressed — not from this change; verified by running my tests in isolation.)
