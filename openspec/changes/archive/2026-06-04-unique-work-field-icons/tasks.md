## 1. Icon Mapping

- [x] 1.1 Add a pure work-field icon path map keyed by `DomainGroupId`.
- [x] 1.2 Replace the modulo-based `DomainIcon` renderer with the keyed icon path.
- [x] 1.3 Ensure chip SVGs remain `currentColor` based and accessible as decorative icons.

## 2. Verification

- [x] 2.1 Add unit coverage that every domain has an icon and no icon path is duplicated.
- [x] 2.2 Run the relevant unit tests.
- [x] 2.3 Confirm OpenSpec status is complete for `unique-work-field-icons`.
